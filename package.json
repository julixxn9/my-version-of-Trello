{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run build",
    "deploy": "gh-pages -d dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.86.0"
  },
  "dependencies": {
    "extract-uri-image": "^1.1.0",
    "vite": "^6.3.4"
  }
}
